global:
  resolve_timeout: 1m

route:
  receiver: "default-receiver"

receivers:
  - name: "default-receiver"
    webhook_configs:
      - url: "http://localhost:5001/alert"
        send_resolved: true
